def depositar (self, valor):
    if valor > 0
       self saldo += valor 
       return True
    return False
def sacar (self, valor)
    if valor > 0 and valor <= self.saldo:
       self saldo -= valor
       return True
    return False
def consutar_salto(self):
    return self.saldo
